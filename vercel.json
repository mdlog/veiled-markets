{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "ls -la && pwd && cd frontend && npm install && npm run build",
    "outputDirectory": "frontend/dist",
    "installCommand": "echo 'Dependencies will be installed in build command'",
    "framework": null
}