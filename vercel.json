{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "ls -la node_modules/.bin/ && npm run build",
    "outputDirectory": "dist",
    "installCommand": "npm install --include=dev && ls -la node_modules/.bin/",
    "framework": "vite"
}