{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "pnpm --filter @veiled-markets/frontend build && ls -la && ls -la dist || echo 'dist not found'",
    "installCommand": "pnpm install",
    "outputDirectory": "dist"
}